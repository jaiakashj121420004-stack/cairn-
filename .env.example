# Cairn — environment variables
# No secrets needed for v1 (everything is local)
# Copy to .env for local overrides (not committed)

# NODE_ENV is set automatically by electron-vite
# NODE_ENV=development
