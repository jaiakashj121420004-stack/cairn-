export const CHART_COLORS = {
  win: 'hsl(var(--accent-a))',
  loss: 'hsl(var(--danger))',
  neutral: 'hsl(var(--accent-b))',
  warning: 'hsl(var(--warning))',
  axis: 'hsl(var(--text-muted))',
  grid: 'hsl(var(--border))',
  surface: 'hsl(var(--surface-elevated))',
  text: 'hsl(var(--text-primary))',
}
